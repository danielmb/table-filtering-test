import React from 'react';
const NavbarUser = async () => {
  // const user = await currentUser();
  // if (!user) return null;
  return (
    <div className="flex items-center space-x-4 bg-background text-foreground"></div>
  );
};

export default NavbarUser;
